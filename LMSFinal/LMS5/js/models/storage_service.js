export default class StorageService {
  constructor(data, y, entity, entitySingle, options) {
    this.data = data;
    this.entity = entity;
    this.entitySingle = entitySingle;
    this.options = options;
  }
}
